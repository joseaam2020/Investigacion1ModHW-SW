1.The new interface should have the Construction Canvas (Center: Construction Canvas
Large canvas with a stylized ESP32 DevKit visual (center) and a grid/graph paper background.
Left/right vertical lists of pin items representing board pins (IO34, IO35, IO32, …, TX0, RX0, IO21, etc.).
A device card overlay showing selected module details (e.g., LED card with Pin: 34).
Visual connector placeholders and repeated-linear-gradient background for the canvas effect.) from Retro Educational Dashboard to the right. 

2. On the left side of the screen i would like a left side bar with the contents of pallete and workspace of index.html.

3. Header from Retro Educational Dashboard Design with the following changes: No simulate button. The save button should save the same code as the one shown in prewiew (which i think is generated in the backend) and saved into a .cpp file an saved in downloads.